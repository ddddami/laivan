type: object
required:
  - id
  - email
  - display_name
  - status
properties:
  id:
    type: string
    format: uuid
  email:
    type: string
    format: email
  display_name:
    type: string
  status:
    type: string
    enum:
      - active
      - suspended
